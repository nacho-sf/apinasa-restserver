const Landing = require('./landing');
const Nea = require('./nea');
const Role = require('./role');
const Server = require('./server');
const User = require('./user');

module.exports = {
    Landing,
    Nea,
    Role,
    Server,
    User
};